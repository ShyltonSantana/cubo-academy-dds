const nomes = "campana";
let soma = 0;
let encontrado = false;


for (let letra of nomes) {
    if (letra === "a") {
        console.log('tem letra a');
        encontrado === true;
        break;
    }
} if (!encontrado === false) {
    console.log("não tem a");
}
